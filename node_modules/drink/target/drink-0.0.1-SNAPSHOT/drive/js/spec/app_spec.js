( function() {
  var dependencies, specUnit;

  specUnit = [ 'spec/unit/models/widget_spec' ];

  dependencies = [ 'chai', 'sinonChai' ].concat( specUnit );

  define( dependencies, function( chai, sinonChai ) {
    chai.should();
    return chai.use( sinonChai );
  } );

} ).call( this );
