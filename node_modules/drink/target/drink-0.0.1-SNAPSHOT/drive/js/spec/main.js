( function() {
  define( [ 'spec/app_spec' ], function() {
    if ( window.mochaPhantomJS ) {
      return window.mochaPhantomJS.run();
    } else {
      return window.mocha.run();
    }
  } );

} ).call( this );
